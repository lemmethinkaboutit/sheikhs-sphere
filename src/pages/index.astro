---
import { type CollectionEntry } from "astro:content";
import PostPreview from "@/components/blog/PostPreview.astro";
import SocialList from "@/components/SocialList.astro";
import { getAllPosts } from "@/data/post";
import PageLayout from "@/layouts/Base.astro";
import { collectionDateSort } from "@/utils/date";

// Posts
const MAX_POSTS = 10;
const allPosts = await getAllPosts();
const allPostsByDate = allPosts.sort(collectionDateSort) as CollectionEntry<"post">[];
const latestPosts = allPostsByDate.slice(0, MAX_POSTS);

// Pinned Posts, set to a max of 3
const MAX_PINNED_POSTS = 3;
const pinnedPosts = allPostsByDate
	.values()
	.filter((p) => p.data.pinned)
	.take(MAX_PINNED_POSTS)
	.toArray();
---

<PageLayout meta={{ title: "Home" }}>
	<section>
	<h1 class="title title-gradient mb-3 text-3xl sm:text-4xl">Sheikh's Sphere</h1>
	<p class="text-muted text-xs mb-10 tracking-widest uppercase">Business Process Automation · ServiceNow SPM &amp; IRM · Marketing &amp; Growth</p>
	<div class="flex flex-col sm:flex-row items-start gap-8 mb-8">
		<div class="flex-1">
			<p class="mb-4">
				I automate business processes — and unlike most people who do that, I've personally made the cold calls, run the campaigns, and carried the pipeline those processes are supposed to serve.
			</p>
			<p class="mb-4">
				Five years across sales floors, cement factories, and ServiceNow platforms taught me how businesses actually work — not the MBA version, the real one.
			</p>
			<p class="mb-4">
				Today I help organizations automate the work that drags them down: demand intake, portfolio governance, risk and compliance on ServiceNow SPM &amp; IRM — paired with marketing and sales strategy, because automation only matters if it moves revenue. CSA and CIS-SPM certified. Available for consulting and project work.
			</p>
			<p class="mb-6">
				This is where I write honestly about the journey.
			</p>
			<div class="mb-8 flex flex-wrap gap-3">
				<a href="/services/" class="btn-glow text-xs px-4 py-2 rounded-lg bg-accent/15 text-accent border border-accent/30 hover:bg-accent/25 transition-colors font-semibold">See what I do →</a>
				<a href="mailto:magic.alive11@gmail.com" class="text-xs px-4 py-2 rounded-lg border border-muted/30 hover:border-accent hover:text-accent transition-colors font-semibold">Email me</a>
			</div>
			<SocialList />
		</div>
		<div class="flex-shrink-0">
			<img
				src="/profile.png"
				alt="Abdul Hadi Sheikh"
				class="w-40 h-40 rounded-2xl object-cover border-2 border-accent profile-img"
			/>
		</div>
	</div>
</section>

	{
		pinnedPosts.length > 0 && (
			<section class="mt-16">
				<h2 class="title mb-6 text-xl">Pinned Posts</h2>
				<ul class="space-y-4" role="list">
					{pinnedPosts.map((p) => (
						<li class="grid gap-1 sm:grid-cols-[auto_1fr]">
							<PostPreview post={p} />
						</li>
					))}
				</ul>
			</section>
		)
	}

	<section class="mt-16">
		<h2 class="title text-accent mb-6 text-xl">
			<a href="/posts/">Blog</a>
		</h2>
		<ul class="space-y-4" role="list">
			{
				latestPosts.map((p) => (
					<li class="grid gap-1 sm:grid-cols-[auto_1fr]">
						<PostPreview post={p} />
					</li>
				))
			}
		</ul>
	</section>

	<section class="mt-16">
		<h2 class="title text-accent mb-6 text-xl">
			<a href="/projects/">Projects</a>
		</h2>
		<p class="text-sm">
			Case studies from real work — marketing strategy, brand projects, and platform builds.
			<a href="/projects/" class="text-accent hover:underline">View all projects →</a>
		</p>
	</section>
</PageLayout>